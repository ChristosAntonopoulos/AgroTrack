# OliveCycle Alpha APK

Place the signed Android build here as:

```
olivecycle-alpha.apk
```

The landing page download links point to `/downloads/olivecycle-alpha.apk`.

Build from the mobile app:

```bash
cd mobile
eas build --platform android --profile preview
```

Or export locally with Expo and copy the APK to this folder.
