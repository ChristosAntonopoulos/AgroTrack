apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: agrotrack-backend
  labels:
    app: olive-lifecycle-api
spec:
  type: NodePort
  selector:
    app: olive-lifecycle-api
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31847
    protocol: TCP
    name: http
